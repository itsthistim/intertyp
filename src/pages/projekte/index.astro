---
import Layout from "@/layouts/Layout.astro";
import Nav from "@/components/Nav.astro";
import Footer from "@/components/Footer.astro";
import CardGrid from "@/components/CardGrid.astro";

const res = await fetch(new URL("/api/projekte", Astro.url));
let projects = await res.json();
---

<Layout title="Dienstleistungen | Intertyp GmbH">
	<Nav />
	<main>
		<CardGrid
			title="Alle Projekte"
			description="Schauen Sie sich unsere neuesten Projektzusätze und Referenzen an."
			items={projects}
			errorMessage="Keine Projekte gefunden."
		/>
	</main>
	<Footer />
</Layout>

<style>
	main {
		max-width: 1200px;
		margin: 2rem auto;
		padding: 0 1rem;
	}
</style>
